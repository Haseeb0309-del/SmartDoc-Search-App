# System Test Design and Cross-Validation Document

This document strictly follows the TCS iON Industry Project Test Design Specification template.

## Test Design Matrix

| Test Case # | Test Step # | Application / Screen | Test Case | Pre-Requisites | Input provided to the data analysis | Iteration # NN | Cross-Validation Method | Actual result | Defect[Y/N] |
|---|---|---|---|---|---|---|---|---|---|
| TC01 | 1 | Search Results / Portal | Verify natural language intent search processing using semantic embeddings | Policy dataset `employee_docs.csv` cleaned and indexed into `local_search_index.pkl` | Query: "how do I work securely from home", Filters: ["IT", "HR"], Top: 10 | 1 | Compare sentence-transformer cosine similarity score against Azure AI Search ranking score | Document "Remote Work Policy" & "VPN Setup Guide" retrieved with score 0.388+, categorized under IT | N |
| TC02 | 1 | Search Bar / Input | Verify real-time autocomplete suggestions on search input | Server backend online, autocomplete endpoint `/autocomplete` operational | Input prefix: "work", Top suggestions: 5 | 1 | Validate prefix matching against indexed document terms and Azure suggest API | Suggestions dropdown opens displaying "working hours standard working" and "work eligible employees may" | N |
| TC03 | 1 | Search Results / Filter | Verify dynamic sorting by relevance score and modification date | Search results active on screen | Sort option: "date", Query: "policy" | 1 | Compare client-side array sort against backend database TIMESTAMP order | Search result cards re-order dynamically by last updated date in descending order | N |
| TC04 | 1 | Results Pagination | Verify pagination controls for search query matching over 10 items | Search matches > 10 policy documents | Navigation action: Click "Next", Page: 2, Top: 10 | 1 | Validate total count calculation and page offset slicing `(page - 1) * top` | Page 2 loads 10 items, Previous button enabled, page indicator shows "Page 2 of N" | N |
| TC05 | 1 | Document Detail View | Verify policy document text export and local file download | User navigating active document detail view | Click "Download .txt" button, Doc ID: "hr-2" | 1 | Compare generated `.txt` file checksum and line contents against source database text | Plain text file `Remote_Work_Policy.txt` downloaded to local machine with formatted sections | N |
| TC06 | 1 | ML Classifier / Admin | Verify machine learning document classifier categorization accuracy | Model `smart_doc_classifier.pkl` loaded into Flask server | Input snippet: "Cisco AnyConnect VPN installation failover and firewall rule reset" | 1 | 5-Fold Stratified Cross-Validation on TF-IDF + LinearSVC pipeline (`evaluate_model.py`) | Classifier outputs category "IT" with 100% precision and overall 94.7% accuracy | N |
| TC07 | 1 | Auth Portal / Login | Verify user registration, password hashing, and authentication workflow | SQLite database `users.db` initialized with users table schema | Registration input: Name="Test User", Email="user@nexussolutions.in", Pass="securepass123" | 1 | Verify SHA-256 password hash comparison in SQLite DB table against form payload | User registered successfully, logged in, and user profile session stored in state | N |
